

export enum AppEnv {
    DEV = "development",
    PROD = "production",
    STG = "staging",
    TEST = "test",
}

export enum LogLevel {
    DEBUG = "debug",
    INFO = "info",
    WARN = "warn",
}
